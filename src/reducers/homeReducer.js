
let defaultState = {
    home_sliders: []
}

const homeReducer = (state = defaultState, action) => {
    switch (action.type) {
      default:
        return {
            ...state
        }
    }
}

export default homeReducer